import { KgToPoundPipe } from './kg-to-pound.pipe';

describe('KgToPoundPipe', () => {
  it('create an instance', () => {
    const pipe = new KgToPoundPipe();
    expect(pipe).toBeTruthy();
  });
});
